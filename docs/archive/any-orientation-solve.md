## Any Orientation Solve Checklist

- [x] Task A — `game/jigsaw_solver.lua` — Replace the body of `M.is_assembled` (currently lines 5-22) with a rotation-aware version per the design doc's "Shape of the new function": add a `M.rotate_cell(row, col, k)` helper (or equivalent inlined logic) implementing the 90°-step rotation table — `k=0: (col, row)`, `k=1: (-row, col)`, `k=2: (-col, -row)`, `k=3: (row, -col)` (mod 4) — and rewrite `is_assembled` to read `k = pieces[1].rotation_step` once, reject if any other piece's `rotation_step ~= k` (replacing the old blanket `piece.rotation_step ~= 0` rejection on line 10), compute `gx, gy = M.rotate_cell(piece.row, piece.col, k)` in place of the raw `piece.col`/`piece.row` used in the current `px`/`py` subtraction (lines 12-13), and keep the existing shared-offset `(ox, oy)` relative-consistency check unchanged otherwise. Piece-count check (`#pieces ~= expected_count`, line 6) stays first and unchanged. Independent — no dependencies.

- [x] Task B — `tests/test_jigsaw.lua` — Update and extend the `is_assembled` tests (lines 1093-1151). **Depends on Task A** (needs the final helper name/shape, e.g. `JigsawSolver.rotate_cell`, to write fixtures against). Specifically:
  - Change the "false when a piece is rotated" case (lines 1122-1128) to "false when pieces disagree on rotation": keep `build_assembled_pieces(0, 0)` but set only `pieces[1].rotation_step = 1` (leaving the rest at 0) and update the assertion message/print text accordingly (mixed rotation must still fail).
  - Add a new case: a fully-assembled layout with every piece sharing the same non-zero `rotation_step` (test at least step 1; consider looping steps 1-3), with each piece's `sprite.x/y` positioned per the rotated-grid mapping (`M.rotate_cell(row, col, k)` + a shared offset), asserting `is_assembled(...) == true`.
  - Add a new case: every piece shares the same non-zero `rotation_step`, but `sprite.x/y` is still laid out using the *unrotated* (`k=0`) mapping — rotation and layout disagree — asserting `is_assembled(...) == false`.
  - Repeat the existing "two pieces swapped" case (lines 1130-1141) and "constant offset" case (lines 1143-1151) at a non-zero shared rotation step (e.g. all pieces at `rotation_step = 1`, positioned via the rotated mapping) to confirm swap-detection and offset-invariance still hold once rotated.
  - Add a brief comment near the existing solve→vanish integration tests (~line 1153+) noting that solving-while-rotated is covered at the unit level here, not duplicated in the integration tests (which stay at `rotation_step = 0`).

- [x] Task C — `README.md` — Update the two references to the old all-unrotated-only rule. **Depends on Task A** (wording should reflect the actual resolved behavior/terminology chosen). Specifically:
  - Line 12: change "right rotation, right relative position" to describe that the pieces must share one consistent rotation (0°, 90°, 180°, or 270° as a whole) and be in the correct relative position — i.e. the whole assembled picture may be solved rotated as a rigid unit, not only fully upright.
  - Line 25: change the `jigsaw_solver.lua` structure blurb from "all unrotated and in correct relative arrangement" to reflect that all pieces must share one consistent rotation step (any of the four 90°-steps) and be in correct relative arrangement under that rotation.
