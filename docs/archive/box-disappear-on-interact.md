# Box Disappear On Interact Checklist

- [x] Task A — `game/jigsaw_box.lua` — Three changes to `JigsawBox`:
  1. Change `JigsawBox.new(x, y)` (line 14) to `JigsawBox.new(x, y, world_w, world_h)`. Store `self.world_w = world_w` and `self.world_h = world_h` (e.g. right after `self.spawn_timer = 0` on line 19).
  2. In `JigsawBox:interact()` (lines 43-48), inside the `if self.state == "waiting" then` branch, add `self.sprite.visible = false` alongside the existing `self.state = "ejecting"` and `self.spawn_timer = 0` lines, so the sprite hides on the same frame `state` flips.
  3. In `_eject_next` (lines 59-113), inside the candidate loop (`for _, pair in ipairs(candidates) do`, lines 81-95), after computing `local tx = bx + pair[1] * C.SLOT` / `local ty = by + pair[2] * C.SLOT`, add an out-of-bounds check: `local out_of_bounds = tx < 0 or tx >= self.world_w or ty < 0 or ty >= self.world_h`. Change the final acceptance condition from `if not occupied then` to `if not occupied and not out_of_bounds then`. Do not touch the `d = 1, 20` loop bounds, the candidate-generation/sort logic, or the occupied-slot check itself — a candidate must now pass both checks to be accepted. The box's own tile (`d = 0`) is already excluded since the loop starts at `d = 1`; leave that as-is.

- [x] Task B — `game/scenes/game_scene.lua` — **Depends on Task A** (needs the new `JigsawBox.new` signature to exist first). Update both call sites to pass world bounds:
  1. In `on_enter()` (line 38), change `self.boxes = { JigsawBox.new(5 * C.SLOT, 3 * C.SLOT) }` to `self.boxes = { JigsawBox.new(5 * C.SLOT, 3 * C.SLOT, self.world_w, self.world_h) }`. (`self.world_w`/`self.world_h` are already assigned on lines 22-23, above this line, so they're available.)
  2. In `_spawn_box()` (line 65), change `local box = JigsawBox.new(cx, cy)` to `local box = JigsawBox.new(cx, cy, self.world_w, self.world_h)`.
  No other changes in this file — `GameScene:update`'s box-removal loop (lines 85-91) keeps its `box.state == "done"` gate and its `box.sprite.visible = false` line exactly as-is (now a harmless no-op by the time it runs).

- [x] Task C — `tests/test_jigsaw.lua` — **Depends on Task A and Task B.** Two parts:
  1. **Fix existing call sites that trigger ejection.** Any existing `JigsawBox.new(...)` call in this file that is followed by a `box:update(...)` call (which reaches `_eject_next`, and will now compare `tx`/`ty` against `self.world_w`/`self.world_h`) must pass generous world bounds so `world_w`/`world_h` aren't `nil` and none of the existing assertions change behavior. Update these specific blocks to pass `2000, 2000` as trailing args (e.g. `JigsawBox.new(128, 128, 2000, 2000)`): the "update() ejects one piece per call" block (~line 310), the "update() x9 ejects all pieces, state becomes done" block (~line 322), the "slot search skips occupied slots" block (~line 339, box at `128, 192`), the "ejected piece gets a random initial rotation_step" block's 12-trial loop (~line 445), and the "ejected piece carries non-nil image + quad" block (~line 469). Leave every other `JigsawBox.new(...)` call site in the file untouched — the plain `new()`/`interact()` unit tests (~line 291, ~300), the quad-slicing/shuffle/newImage tests (~379, ~420, ~499), the row/col match test (~784), and the nearest-box / box-vs-button-priority tests (~1047-1049, ~1103) never call `box:update()` so never reach `_eject_next` and don't need `world_w`/`world_h`.
  2. **Add three new test blocks**, following this file's existing style (plain `do ... end` blocks with `assert` + a `print("PASS: ...")` line), placed near the existing `JigsawBox` tests (after ~line 356, the "slot search skips occupied slots" block):
     - **Immediate invisibility:** construct `local box = JigsawBox.new(128, 128, 2000, 2000)`, assert `box.sprite.visible == true` before interacting, call `box:interact()`, then assert `box.sprite.visible == false` and `box.state == "ejecting"` (not `"done"`) — proving the sprite hides on the same call that starts ejection, not after all 9 pieces are out.
     - **Background ejection unaffected:** construct `local box = JigsawBox.new(128, 128, 2000, 2000)`, call `box:interact()`, then loop `box:update(1.0, pieces)` 9 times exactly like the existing x9 test (~line 322), additionally asserting `box.sprite.visible == false` after every single update call (not just the first), and finally asserting `#pieces == 9` and `box.state == "done"` — proving invisibility from `interact()` doesn't interrupt or alter the existing eject cadence/count.
     - **World-bounds respected near an edge:** construct a box at the world corner with a small-but-sufficient world, e.g. `local world_w, world_h = 4 * C.SLOT, 4 * C.SLOT` and `local box = JigsawBox.new(0, 0, world_w, world_h)` (16 total slots, comfortably more than the 9 pieces to eject so the search never exhausts valid cells). Call `box:interact()`, then `box:update(1.0, pieces)` 9 times into a shared `pieces` table, assert `box.state == "done"` at the end (proving the bounds check doesn't stall the search), and assert every entry in `pieces` satisfies `p.sprite.x >= 0 and p.sprite.x < world_w and p.sprite.y >= 0 and p.sprite.y < world_h` — proving no piece was ejected off the playable ground.
