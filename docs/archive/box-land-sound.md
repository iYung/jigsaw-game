## Box Land Sound Checklist

- [x] Task A — `game/jigsaw_box.lua` — Add `local Sound = require("lua/core/sound")` to the top require block (alongside the existing `Sprite`, `JigsawPiece`, `C`, `PuzzleCatalog`, `GameState` requires, lines 1-5). Then, in `JigsawBox:update`, inside the `"flying"` branch, at the point where `self.fly_timer <= 0` (currently lines 100-102) — the single, non-repeating moment the box snaps to `self.target_x, self.target_y` and sets `self.state = "waiting"` — add a call to `Sound.play("put_down")`. This is the only code change needed: `put_down` is already registered in `main.lua`'s `SFX_MANIFEST.sfx` and loaded by `lua/core/sound.lua`, so no new asset or manifest entry is required. Do not add the sound call anywhere else (e.g. not in the initial non-flying "waiting" spawn path in `JigsawBox.new`, which must remain silent per the design).
