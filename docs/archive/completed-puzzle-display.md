## Completed Puzzle Display Checklist

- [x] Task A — `game/jigsaw_box.lua` — In `JigsawBox.new` (around line 36, right after `local puzzle_image = love.graphics.newImage(path)`), add `self.image = puzzle_image` so the loaded image survives on the box instance past quad-building, letting callers read `box.image` later. No other changes to `JigsawBox` (piece-cutting, shuffling, ejection all stay the same). Independent — no dependencies.

- [x] Task B — `game/scenes/game_scene.lua` — Add `image`/`cols`/`rows` fields to both places an `active_puzzles` entry is constructed: in `on_enter()` (currently lines 62-66, `self.active_puzzles[#self.active_puzzles + 1] = {pieces = box.spawned, piece_count = box.piece_count, solved = false}`) and in `_spawn_box()` (currently lines 100-104, same shape). Change both to also set `image = box.image, cols = box.cols, rows = box.rows`, so the entry carries everything needed to draw the puzzle on the shelf after the `JigsawBox` itself is gone. (Depends on Task A — `box.image` must exist.)

- [x] Task C — `game/scenes/game_scene.lua` — In `on_enter()`, initialize `self.completed_puzzles = {}` alongside the existing `self.active_puzzles = {}` (currently line 52). This is the new permanent, never-pruned list backing the trophy shelf. No other logic in this task — just the initialization. Independent — can run in parallel with Task A/B (touches a different line in the same file; apply first or reconcile trivially if edits land out of order).

- [x] Task D — `game/scenes/game_scene.lua` — In `update()`'s existing fade-prune loop (currently lines 155-169, the `for i = #self.active_puzzles, 1, -1 do ... if all_faded then table.remove(self.active_puzzles, i) end` block), right before `table.remove(self.active_puzzles, i)`, add logic to shelve the completed puzzle:
  - Compute `y = -(C.SLOT + entry.rows * C.SLOT)` (the image's bottom edge lands one `C.SLOT` above `y = 0`; the image extends upward from there).
  - Compute `x` as the running sum of every previously-shelved puzzle's pixel width plus a `C.SLOT` gap: the first shelved puzzle starts at `x = 0`; each subsequent one starts at `prev_x + prev_cols * C.SLOT + C.SLOT` (derive this from `self.completed_puzzles`'s existing entries — e.g. track cumulative width, or read the last entry's `x`/`cols` — rather than a fixed slot width, since `cols`/`rows` vary by difficulty tier).
  - Build a drawable table `{image = entry.image, x = x, y = y, cols = entry.cols, rows = entry.rows, draw = function(self) love.graphics.draw(self.image, self.x, self.y) end}`, append it to `self.completed_puzzles`, and call `self.drawer:add(that_drawable, C.PRIORITY_PIECE)` (matching the existing pattern used for pieces/boxes elsewhere in this file, e.g. line 61/71/99/118).
  - No changes to `draw()` — the shelf renders through the existing `Scene.draw()` → `camera:attach()` → `drawer:draw()` pipeline.
  - (Depends on Task B — needs `entry.image`/`entry.cols`/`entry.rows` on `active_puzzles` entries — and Task C — needs `self.completed_puzzles` to append to.)

- [x] Task E — `tests/test_jigsaw.lua` — Add test coverage for the new behavior, following this file's existing conventions (`GameState:reset()` before each `do...end` block that constructs a box, the `new_easy_box` helper for deterministic 3x3/9-piece grids, and the integration-style `GameScene.new()`/`gs:on_enter()`/`gs:update(dt)` pattern already used in the "integration: assembling the puzzle vanishes pieces..." block near line 1147). Add at least:
  - A test that `box.image` is non-nil after `JigsawBox.new(...)` (e.g. via `new_easy_box`), confirming Task A.
  - A test that an `active_puzzles` entry produced by `GameScene:on_enter()` (or `_spawn_box()`) carries non-nil `image` and correct `cols`/`rows` matching the spawned box, confirming Task B.
  - An integration test that drives a `GameScene` through the same solve-and-fade flow as the existing "integration: assembling the puzzle..." block (seed 9 correctly-arranged pieces + a matching `active_puzzles` entry carrying `image`/`cols`/`rows`, call `gs:update(1/60)` to trigger `solved = true`/vanish, then `gs:update(C.PIECE_FADE_DURATION)` to complete the fade) and asserts that afterward `#gs.completed_puzzles == 1` and the new entry's position matches the resolved design formula: `y == -(C.SLOT + rows * C.SLOT)` and `x == 0` for the first-ever shelved puzzle (bottom edge one `C.SLOT` above `y = 0`, images growing upward). Optionally extend with a second solved-and-faded puzzle to assert its `x` equals the first entry's `cols * C.SLOT + C.SLOT` gap offset, per the design's left-to-right accumulation rule.
  - (Depends on Tasks A, B, C, D — exercises the fully implemented feature end-to-end.)

- [x] Task F — `README.md` — Add a brief mention of the trophy shelf, matching the tone/placement of the existing seen-tracking and 3-puzzle-cap sentences in the "Gameplay" section (currently the paragraph starting "A red **spawn button**..." around line 14, which already documents per-tier seen-tracking, the 3-puzzle active cap, and the solved-count tracker). Add a sentence noting that once a solved puzzle finishes fading out, its fully-assembled image is permanently displayed at actual size on a "trophy shelf" above the top of the world, accumulating left-to-right in solve order for the rest of the session (visible by walking toward the top edge; purely decorative, no collision/pickup). Independent of the other tasks (content matches the finalized design doc regardless of implementation order), but best done after Task D lands so the wording can be checked against the actual behavior if desired.
