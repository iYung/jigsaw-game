## Escape Saves and Returns to Start Menu Checklist

- [x] Task A — `main.lua` — Refactor the save-writing logic out of `love.quit()` (currently `function love.quit() if manager.current and manager.current.to_save then Save.write({ game_state = GameState:to_save(), scene = manager.current:to_save() }) end end`) into a local function `local function _save_current() ... end` placed above `love.keypressed` (currently `function love.keypressed(key) if key == "escape" then love.event.quit() end end`, around line 62-65), containing the same body. Change `love.quit()` to just call `_save_current()`. Change `love.keypressed(key)` so that on `key == "escape"`: if `manager.current and manager.current.to_save` (i.e. currently in `GameScene`, duck-typed the same way `love.quit()` already checks), call `_save_current()` then `manager:switch(StartScene.new(manager))` (returns to the start menu, which re-reads `Save.exists()` in its own `on_enter()` so Continue becomes available immediately); otherwise (e.g. currently in `StartScene`), keep today's behavior: `love.event.quit()`. No other files change — `StartScene`/`GameScene` need no modifications, `SceneManager:switch` already calls `on_enter()`/`on_exit()` appropriately. There is no dedicated test file for `main.lua` (headless mode never defines `love.keypressed`/`love.quit`, per `lua/headless/runner.lua`'s early return), so this task's own verification is: run `xvfb-run -a love . --headless` from `/root/jigsaw-game` to confirm the full existing suite still passes (this task cannot regress it, since headless mode never loads this code path, but confirm nothing else broke), then do a quick manual/visual sanity read of the diff to confirm the logic matches this description exactly.
