## Floor PNG Checklist

- [x] Task A — `assets/backgrounds/floor.png` — Generate a new 1280x640 placeholder PNG (flat/subtle solid tone, no checkerboard pattern) to serve as the world floor image. Any reasonable neutral ground-like color is fine; this is a placeholder to be replaced with real art later. Use a script (e.g. Python + PIL) to create it; no external assets needed.
- [x] Task B — `game/scenes/game_scene.lua:72-89` — Replace `self.floor`'s procedural `draw` function (the double loop with `love.graphics.rectangle("fill", ...)` alternating gray shades) with an image-based drawable matching the existing `self.background` pattern at `game_scene.lua:63-70`: load `assets/backgrounds/floor.png` via `love.graphics.newImage`, store it as `self.floor.image`, and set `draw = function(self) love.graphics.setColor(1,1,1,1); love.graphics.draw(self.image, 0, 0) end`. Keep `self.drawer:add(self.floor, 0)` unchanged (same priority). Depends on Task A's asset existing at that path.
- [x] Task C — `tests/test_jigsaw.lua:2640-2670` — Update the checkerboard-floor test block: change the `pcall(function() gs.floor.draw() end)` call to `pcall(function() gs.floor:draw() end)` (colon call, matching the `gs.background:draw()` pattern at line 2750) since the new `draw` function reads `self.image`. Update the block's header comment (currently references "checkerboard floor") to describe the image-based floor instead. Keep the existing structural assertions (`gs.floor` is a table, has a `draw` function, registered in drawer at priority 0) — those still apply. Depends on Task B's code shape.
