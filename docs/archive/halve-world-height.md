## Halve World Height Checklist

- [x] Task A — `game/scenes/game_scene.lua` and `tests/test_jigsaw.lua` — Change `WORLD_H` in `game_scene.lua:21` from `20 * C.SLOT` (1280px) to `10 * C.SLOT` (640px), leaving `WORLD_W` on line 20 untouched. Then update the matching assertion in `tests/test_jigsaw.lua:1895` from `assert(gs.world_h == 20 * C.SLOT, ...)` to `assert(gs.world_h == 10 * C.SLOT, ...)`, leaving the `gs.world_w == 20 * C.SLOT` assertion on line 1894 untouched. Both edits are done together in one task because the test assertion directly encodes the constant's value — updating the test without updating the constant (or vice versa) leaves the test suite red, so there is no independent, parallelizable way to split this pair of one-line edits. No other file needs to change: the checkerboard floor loop, `_spawn_box`'s random cell picker, the player's vertical clamp, and `JigsawBox`'s out-of-bounds check all derive from `self.world_h`/`WORLD_H` rather than a second hardcoded constant, per the design doc's "Affected files" and "What stays the same" sections. Do not add camera clamping — the resulting gap between the 640px world and the 720px window is explicitly out of scope for this feature.
