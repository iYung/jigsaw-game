## Menu UI PNGs Checklist

- [x] Task A — `assets/ui/panel_normal.png`, `assets/ui/panel_selected.png` — Generate two small (64x64) flat solid-color placeholder PNGs via a throwaway Python + PIL script (not committed, same precedent as `floor.png`): `panel_normal.png` colored to match today's `NORMAL_COLOR = {0.35,0.35,0.35,1}` (i.e. RGB 89,89,89, fully opaque), `panel_selected.png` colored to match `SELECTED_COLOR = {0.55,0.55,0.55,1}` (RGB 140,140,140, fully opaque). No border, gradient, or rounded corners — flat fill only.
- [x] Task B — `game/scenes/settings_scene.lua` — Add module-level `PANEL_NORMAL`/`PANEL_SELECTED` image loads (near `:39-40`, loading `assets/ui/panel_normal.png`/`assets/ui/panel_selected.png`). In `:draw()`'s row loop (`:309-322`), replace the `rectangle("fill", x, y, w, h)` call with `love.graphics.setColor(1,1,1,1); love.graphics.draw(image, x, y, 0, w/image:getWidth(), h/image:getHeight())`, picking `PANEL_SELECTED` when `i == self.selected` else `PANEL_NORMAL`. Delete the now-unused `NORMAL_COLOR`/`SELECTED_COLOR` locals (`:39-40`). Depends on Task A's assets existing at that path.
- [x] Task C — `game/scenes/start_scene.lua` — Same image-load addition (near `:19-20`). In `:draw()` (`:182-215`), replace both the disabled-Continue branch's rectangle fill (`:194-200`, draw `PANEL_NORMAL` at `setColor(1,1,1,0.4)`) and the normal/selected branch's rectangle fill (`:201-211`, draw `PANEL_SELECTED` or `PANEL_NORMAL` at full alpha) with image draws, same scaling formula as Task B. Delete the now-unused `NORMAL_COLOR`/`SELECTED_COLOR` locals (`:19-20`). Depends on Task A.
- [x] Task D — `game/scenes/controller_select_scene.lua` — Same image-load addition (near `:11-12`). In `:draw()`, replace the three panel fills — Player 1 (`:178-179`, `PANEL_NORMAL`), middle "Devices" legend (`:188-189`, `PANEL_SELECTED`), Player 2 (`:197-198`, `PANEL_NORMAL`) — with image draws, same scaling formula. Delete the now-unused `NORMAL_COLOR`/`SELECTED_COLOR` locals (`:11-12`). Depends on Task A.
- [x] Task E — `tests/test_settings_scene.lua`, `tests/test_start_scene.lua`, `tests/test_controller_select_scene.lua` — Add one smoke-test each asserting `scene:draw()` (opened/entered as needed per that scene's existing test setup) doesn't error under the headless `love.graphics` stub, now that `draw()` calls `love.graphics.newImage`/`draw` instead of only `rectangle`. Depends on Tasks B, C, D landing first.
