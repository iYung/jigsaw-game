## Multiple Puzzle Images Checklist

- [x] Task A — `scripts/generate_puzzle_images.py` (git rename of `scripts/generate_puzzle_gradient.py`), producing `assets/puzzles/diagonal_3x3.png` and `assets/puzzles/stripes_3x3.png` — Rename `scripts/generate_puzzle_gradient.py` to `scripts/generate_puzzle_images.py` (`git mv`). Expand it so it defines one function per image — `generate_gradient()` (the existing logic, unchanged output, still writing `assets/puzzles/gradient_3x3.png`), `generate_diagonal()`, and `generate_stripes()` — plus a `main()` that calls all three and writes all outputs under `assets/puzzles/`. Implement `generate_diagonal()` per the design doc: 192x192 image, `u = x + y` (range `0..2*(W-1)`), `v = x - y` (range `-(W-1)..(W-1)`), `R = round(255 * u / (2*(W-1)))`, `B = round(255 * (v + (W-1)) / (2*(W-1)))`, `G = 150` fixed. Implement `generate_stripes()` per the design doc: 192x192 image, `band = floor((x + y) / 16)`, even bands → teal `(20, 120, 180)`, odd bands → coral `(220, 90, 60)`. Keep the same deterministic, CLI-less, overwrite-on-run behavior as the original script. After writing the script, actually run it (e.g. `python3 scripts/generate_puzzle_images.py`) so `assets/puzzles/diagonal_3x3.png` and `assets/puzzles/stripes_3x3.png` exist as real 192x192 PNG files on disk, and confirm `assets/puzzles/gradient_3x3.png`'s output is unchanged (e.g. diff against the pre-existing file or re-verify its pixel formula still matches). This task is independent of Task B — it only touches the script and the asset files, not any `.lua` code.

- [x] Task B — `game/jigsaw_box.lua` — Add a module-level constant list (place it near the top of the file, alongside other module-level locals/constants):
  ```lua
  local PUZZLE_IMAGES = {
      "assets/puzzles/gradient_3x3.png",
      "assets/puzzles/diagonal_3x3.png",
      "assets/puzzles/stripes_3x3.png",
  }
  ```
  In `JigsawBox.new`, find the line `local puzzle_image = love.graphics.newImage("assets/puzzles/gradient_3x3.png")` and replace the hardcoded path argument with `PUZZLE_IMAGES[math.random(#PUZZLE_IMAGES)]`, i.e. `love.graphics.newImage(PUZZLE_IMAGES[math.random(#PUZZLE_IMAGES)])`. Do not change anything else in `JigsawBox.new` or elsewhere in the file — slicing into 9 quads, shuffle order, and per-piece random rotation all already derive from the loaded image's actual dimensions and must remain untouched. Note: this task can be written and will run correctly under the headless test suite (`lua/headless/stubs.lua`'s `love.graphics.newImage` stub ignores the path argument and returns a fixed stub image regardless of which of the 3 paths is chosen) even before Task A's PNG files exist on disk — so this task is safe to do in parallel with Task A for the purpose of writing/compiling/running headless tests. However, the two new image files from Task A must exist before this change can be verified *visually* (i.e. before running the actual game and confirming the diagonal/stripes images render correctly when randomly selected) — that visual verification step depends on Task A being complete.

- [x] Task C — `tests/test_jigsaw.lua` — depends on Task B (needs `PUZZLE_IMAGES` and the `math.random` selection logic to exist in `game/jigsaw_box.lua` before it can be tested); does NOT depend on Task A, since the headless `love.graphics.newImage` stub ignores the path argument entirely and returns a fixed stub image regardless of whether the real PNG files exist. Add a new test block (near the other `JigsawBox` tests, e.g. after the "ejected piece carries non-nil image + quad" block) that spies on `love.graphics.newImage` for the duration of several `JigsawBox.new()` calls — following the same spy pattern already used in this file for `love.graphics.newQuad` (see the "pieces_to_spawn slices the image into 9 distinct cells" block around line 285) — to capture the path argument passed on each call. Assert that every captured path is one of the 3 expected paths (`"assets/puzzles/gradient_3x3.png"`, `"assets/puzzles/diagonal_3x3.png"`, `"assets/puzzles/stripes_3x3.png"`). Then, following the same probabilistic-variety pattern already used in this file for shuffle order (around line 318, "Probabilistic check that the 9 entries are actually shuffled") and for random initial rotation_step (around line 356), construct several fresh `JigsawBox.new()` instances (e.g. 8-12 trials) and assert that not all captured paths are identical across trials — i.e. verify `math.random` selection actually produces variety rather than always picking the same image. This is a small, additive test; do not modify or remove any existing test blocks.
