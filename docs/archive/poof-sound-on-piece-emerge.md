## Poof Sound on Piece Emerge Checklist

- [x] Task A — `main.lua` — In the `SFX_MANIFEST.sfx` list (currently lines 39-47, inside `main.lua`'s top-level `local SFX_MANIFEST = { sfx_dir = "assets/sounds/", sfx = { ... } }`), add a new entry `"poof"` alongside the existing `"pick_up", "put_down", "fail", "menu_navigate", "menu_confirm", "puzzle_complete", "rotate"` entries, so `Sound.load` registers `assets/sounds/poof.wav`. Independent of all other tasks.

- [x] Task B — `game/jigsaw_box.lua` — Add `local Sound = require("lua/core/sound")` near the other top-of-file `require`s (alongside the existing `local Sprite = require("lua/core/sprite")`, `local JigsawPiece = require("game/jigsaw_piece")`, `local C = require("game/constants")`, `local PuzzleCatalog = require("game/puzzle_catalog")`, `local GameState = require("game/game_state")`). Then, in `JigsawBox:_eject_next(pieces)`, add `Sound.play("poof")` right after the piece is pushed into both tables — i.e. immediately after the existing lines `pieces[#pieces + 1] = piece` and `self.spawned[#self.spawned + 1] = piece` (and before the trailing `if #self.pieces_to_spawn == 0 then self.state = "done" end` block). This mirrors how `game/player.lua` calls `Sound.play("rotate")` directly after `self.held_piece:rotate()`. Call unconditionally, every eject, no debounce, no change to `_eject_next`'s Manhattan-distance slot search or box state machine. Independent of Task A; Task E depends on this task existing (see below).

- [x] Task C — `assets/sounds/poof.wav` — Create the placeholder asset by copying an existing sfx file, e.g. `cp assets/sounds/menu_navigate.wav assets/sounds/poof.wav` (mirroring how `rotate.wav` was created by copying `menu_navigate.wav`). No code changes. Independent of all other tasks.

- [x] Task D — `assets/sounds/attribution.txt` — Add a new line crediting `poof.wav`, following the existing one-line-per-file convention and matching the wording style already used for `rotate.wav`'s line (`rotate.wav is a temporary placeholder, copied from menu_navigate.wav, pending a real sourced sound`). New line should read: `poof.wav is a temporary placeholder, copied from menu_navigate.wav, pending a real sourced sound`. Independent of all other tasks, but should reference whichever source file Task C actually copies from (keep the two consistent if Task C changes source file).

- [x] Task E — `tests/test_jigsaw_box.lua` (new file) — **Depends on Task B being complete first** (the test asserts behavior that only exists once `Sound.play("poof")` is added to `_eject_next`). Create this new test file, following the conventions of `tests/test_player.lua`'s Test 4 (lines ~127-181): require `lua/core/sound` and monkey-patch `Sound.play` to record calls into a table (spy-and-restore via a `with_sound_play_spy(fn)` helper, restoring the original `Sound.play` afterward, matching the `pcall`-wrapped restore pattern in `test_player.lua`), require `game/jigsaw_box.lua`'s `JigsawBox`, construct a box (or otherwise get access to `JigsawBox:_eject_next`) with at least one pending piece spec in `pieces_to_spawn`, invoke the eject path (either by calling `_eject_next(pieces)` directly with a fake/empty `pieces` table, or by driving `update(dt, pieces)` until `spawn_timer` elapses — pick whichever requires the least fake LÖVE/GameState/PuzzleCatalog setup), and assert the spy recorded `"poof"` (use a `played_contains(played, name)` helper like `test_player.lua`'s). Add a print `"PASS: ..."` line per assertion, matching the style of every other test file in `tests/`. If constructing a full `JigsawBox` via `JigsawBox.new` pulls in too much (image loading, `PuzzleCatalog`, `GameState` unlocking, etc.), it is acceptable to build a minimal table with `setmetatable(fake, JigsawBox)` populated with just the fields `_eject_next` reads (`pieces_to_spawn`, `sprite`, `world_w`, `world_h`, `spawned`) and call `JigsawBox._eject_next(fake, pieces)` directly — follow whatever pattern keeps the test focused on the sound trigger, not box setup plumbing.
