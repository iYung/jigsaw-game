## Puzzle Count Tracking Checklist

- [x] Task 1 — `game/game_state.lua` — Add a module constant `GameState.MAX_ACTIVE_PUZZLES = 3` (set on the `GameState` table itself, alongside the existing `GameState.__index = GameState` line). In `GameState.new()`, initialize two new instance fields alongside `self.seen`: `self.solved_count = 0` and `self.active_count = 0`. Add three new methods, placed after the existing `is_tier_exhausted` method and before `reset`:
  - `GameState:puzzle_started()` — increments `self.active_count` by 1. Called once per box spawned into the world (opened or not), regardless of whether it's ever interacted with.
  - `GameState:puzzle_solved()` — increments `self.solved_count` by 1 and decrements `self.active_count` by 1. Called exactly once per puzzle, the instant its arrangement is first detected as correct (not when its pieces finish fading).
  - `GameState:can_start_puzzle()` — returns `self.active_count < GameState.MAX_ACTIVE_PUZZLES` (a boolean). Used to gate spawning before constructing a new box.
  Update `GameState:reset()` to also reset `self.solved_count = 0` and `self.active_count = 0`, alongside its existing `self.seen = {...}` reset — this keeps the existing role of `reset()` as "clear all session state for test isolation" complete. Do not change `mark_seen`, `is_seen`, `unseen_paths`, or `is_tier_exhausted` — they are unaffected by this feature.

- [x] Task 2 — `game/scenes/game_scene.lua` — Add `local GameState = require("game/game_state")` near the top alongside the existing requires (`Scene`, `Sprite`, `Player`, `C`, `JigsawBox`, `JigsawSolver`, `SpawnButton`). Then:
  - In `on_enter()`, the box construction (currently `local box = JigsawBox.new(5 * C.SLOT, 3 * C.SLOT, self.world_w, self.world_h)` followed by unconditionally appending to `self.boxes`/`self.drawer`/`self.active_puzzles` when `box` is non-nil): only call `JigsawBox.new(...)` if `GameState:can_start_puzzle()` returns true (in practice this is always true here since `active_count` starts at 0, but wire it through the same gated path as `_spawn_box()` rather than special-casing the initial box). After a non-nil `box` is successfully appended to `self.boxes`, `self.drawer`, and `self.active_puzzles` (exactly as today), call `GameState:puzzle_started()`.
  - In `_spawn_box()`, add `if not GameState:can_start_puzzle() then return end` as the very first line of the function, before the grid-cell search loop — this makes pressing the spawn button at the cap a silent no-op (no new box, no message, nothing else touched), matching the existing catalog-exhaustion no-op shape (`if not box then return end`, already present after the `JigsawBox.new(...)` call inside the loop). After a box is successfully constructed and appended to `self.boxes`/`self.drawer`/`self.active_puzzles` (existing code, unchanged), call `GameState:puzzle_started()` immediately before the function's `return`.
  - In `update()`, the existing solved-check loop (`for _, entry in ipairs(self.active_puzzles) do if not entry.solved and JigsawSolver.is_assembled(entry.pieces, entry.piece_count) then entry.solved = true ... end end`): immediately after the line `entry.solved = true` (and before the inner loop that calls `piece:start_vanish()` on each piece), add `GameState:puzzle_solved()`. This fires exactly once per entry thanks to the existing `if not entry.solved` guard.
  Nothing else in `game_scene.lua` changes — piece spawning, ejection, fade/prune loops, player update, camera, and draw are all unaffected.

- [x] Task 3 — `tests/test_game_state.lua` (**new file**) — Create a new headless test file (it will be auto-discovered by `lua/headless/runner.lua`'s `tests/*.lua` scan, so no registration elsewhere is needed) following the `do ... end` block-per-scenario style used throughout `tests/test_jigsaw.lua` (each block calls `GameState:reset()` first, since `GameState` is a module-level singleton shared across every test file in the same headless run, and asserts with `print("PASS: ...")` on success so failures surface via the runner's pcall/error mechanism). Cover:
  - `require("game/game_state")` returns the same singleton instance across multiple `require` calls in the same block (matches existing singleton pattern — a quick sanity check, mirroring how `test_jigsaw.lua` already relies on this).
  - After `GameState:reset()`, `GameState.solved_count == 0` and `GameState.active_count == 0`.
  - Calling `GameState:puzzle_started()` N times increments `active_count` by N and leaves `solved_count` unchanged.
  - Calling `GameState:puzzle_solved()` after some `puzzle_started()` calls increments `solved_count` by 1 and decrements `active_count` by 1 per call; calling it multiple times accumulates correctly (e.g. 3 started, 2 solved → `active_count == 1`, `solved_count == 2`).
  - `GameState:can_start_puzzle()` returns `true` when `active_count < GameState.MAX_ACTIVE_PUZZLES` and `false` once `active_count == GameState.MAX_ACTIVE_PUZZLES` (drive this by calling `puzzle_started()` exactly `GameState.MAX_ACTIVE_PUZZLES` times, i.e. don't hardcode `3` — reference the constant so the test doesn't silently drift if the cap value changes) and confirm it flips back to `true` after one `puzzle_solved()` call brings `active_count` back under the cap.
  - `GameState:reset()` zeroes both `solved_count` and `active_count` even after they've been driven to non-zero values by the above.
  - Existing `seen`-tracking behavior (`mark_seen`/`is_seen`/`unseen_paths`/`is_tier_exhausted`) is untouched by these changes — a minimal smoke assertion is enough here since full coverage already exists via `test_jigsaw.lua`'s usage; do not duplicate that coverage.

- [x] Task 4 — `tests/test_jigsaw.lua` — Update/add game-scene-integration coverage for the spawn cap (depends on Task 2's wiring being in place). `GameState` is already required at the top of this file (`local GameState = require("game/game_state")`) and already reset at the top of most `do...end` blocks — follow that existing convention for every new/modified block below:
  - **Update the existing `_spawn_box()` "places boxes on grid-aligned, in-bounds, non-colliding cells" test** (currently ~line 1348-1385: `GameState:reset()`, `gs:on_enter()`, then 20 calls to `gs:_spawn_box()`, asserting `#gs.boxes > boxes_before`): the existing comment above the assertion attributes the no-op behavior to catalog exhaustion (5 images) — this is no longer the binding constraint once the cap is wired in (the cap of `GameState.MAX_ACTIVE_PUZZLES` will be hit first, after only 2 more successful spawns beyond the 1 box `on_enter()` already created). Update the comment to reflect the cap as the actual reason most of the 20 attempts no-op, and add an assertion that `#gs.boxes <= GameState.MAX_ACTIVE_PUZZLES` after the loop. The existing "at least one new box" assertion still holds and needs no change.
  - **Add a new test block "enforces the 3-active-puzzle cap"** placed near the `_spawn_box()` tests: `GameState:reset()`, create a fresh `GameScene.new()`, call `gs:on_enter()` (1 active puzzle from the initial box). Call `gs:_spawn_box()` repeatedly (bounded loop, e.g. up to 20 attempts to allow for grid-collision retries) until `#gs.boxes == GameState.MAX_ACTIVE_PUZZLES`, then assert `GameState.active_count == GameState.MAX_ACTIVE_PUZZLES` and `GameState:can_start_puzzle() == false`. Call `gs:_spawn_box()` once more and assert `#gs.boxes` is unchanged (silent no-op at the cap) and `GameState.active_count` is unchanged. Then simulate solving one of the active puzzles the same way the existing "assembling the puzzle" test does (~line 1145-1225: replace one `active_puzzles` entry's pieces with a correctly-arranged set matching `JigsawSolver.is_assembled`'s contract, call `gs:update(dt)` once) and assert `GameState.active_count` drops by 1, `GameState.solved_count` increases by 1, and `GameState:can_start_puzzle() == true` again; then call `gs:_spawn_box()` once more and assert a new box is successfully added (`#gs.boxes` back up by 1, assuming the catalog still has an unseen image available — reset `GameState` gives a full unseen pool, and this test only needs at most `MAX_ACTIVE_PUZZLES + 2` successful constructions total, well within the 5-image catalog).
  - **Extend the existing "assembling the puzzle fades out..." test(s)** (~line 1145-1225 and the ~1220-1315 two-puzzle variant) with an assertion that `GameState.solved_count` increases by exactly 1 at the moment each puzzle's `entry.solved` flips to `true`, and does not increase again on subsequent frames while its pieces are fading (guards the one-shot-per-entry contract from Task 2's placement of `GameState:puzzle_solved()` inside the existing `if not entry.solved` guard).

### Sequencing

`game_state.lua` (Task 1) has no dependencies and must land first. `game_scene.lua` (Task 2) calls the new `GameState` methods, so it depends on Task 1. `test_game_state.lua` (Task 3) unit-tests Task 1's API directly and doesn't touch `game_scene.lua`, so it only depends on Task 1, not Task 2. `test_jigsaw.lua`'s cap tests (Task 4) exercise `GameScene:_spawn_box()`/`on_enter()`/`update()`, so they depend on Task 2's wiring being in place.

- **Wave 1:** Task 1
- **Wave 2 (parallel):** Task 2, Task 3 — both only need Task 1 done
- **Wave 3:** Task 4 — needs Task 2's wiring in place
