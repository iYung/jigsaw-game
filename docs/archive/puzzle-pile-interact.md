## Puzzle Pile Interact Checklist

Source design doc: `docs/design/puzzle-pile-interact.md`. All five tasks below touch disjoint files and can run in parallel — none depends on another having landed first, since the design doc fully specifies each file's target shape independently.

- [x] Task A — `game/puzzle_pile.lua`, `game/scenes/game_scene.lua`, `game/player.lua` — Core implementation (one agent, tightly coupled — do not split across agents to avoid inconsistency):
  - `game/puzzle_pile.lua`: change `PuzzlePile.new(x, y)` to `PuzzlePile.new(x, y, on_press)`, storing `self.on_press = on_press`. Add `PuzzlePile:interact()` (calls `self.on_press()` if set — identical body to the old `SpawnButton:interact()`) and `PuzzlePile:centre()` (returns `{x = self.sprite.x + C.U, y = self.sprite.y + C.U}` — identical body to the old `SpawnButton:centre()`). Update the header comment at the top of the file (currently starts "Visual-only, no interact() -- same role game/door.lua used to play...") since the pile now has an `interact()`. Leave `count()`, `top_position()`, and `draw()` untouched. Exact code is in the design doc's "`game/puzzle_pile.lua`" section.
  - `game/scenes/game_scene.lua`: remove the `local SpawnButton = require("game/spawn_button")` line (currently line 8). Remove `self.spawn_button = SpawnButton.new(0, 0, function() self:_spawn_box() end)` and `self.drawer:add(self.spawn_button, C.PRIORITY_PIECE)` (currently lines 89–90). Change `self.pile = PuzzlePile.new(WORLD_W / 2, 0)` (currently line 92) to `self.pile = PuzzlePile.new(WORLD_W / 2, 0, function() self:_spawn_box() end)`. In `_spawn_box()`'s occupancy loop, remove the clause `if not occupied and self.spawn_button.sprite.x == cx and self.spawn_button.sprite.y == cy then` (currently line 113) and its body — keep the `self.pile.sprite.x == cx and self.pile.sprite.y == cy` clause (currently line 116) exactly as-is. Change the `player:update()` call (currently line 161) from `self.player:update(dt, self.pieces, self.boxes, self.spawn_button, self.drawer)` to `self.player:update(dt, self.pieces, self.boxes, self.pile, self.drawer)`.
  - `game/player.lua`: in `Player:update(dt, pieces, boxes, button, drawer)` (currently line 26), rename the `button` parameter to `pile` and update its two use sites inside the function body (currently `if self.held_piece == nil and not box_interacted and button ~= nil then` and `local bc = button:centre()` / `button:interact()`, lines 107–113) to use `pile` instead of `button`. No other logic changes — this parameter was already duck-typed to any object exposing `:centre()`/`:interact()`.

- [x] Task B — `game/spawn_button.lua` — Delete this file outright. Nothing else needs to require or reference it once Task A lands (game_scene.lua's `require("game/spawn_button")` line is removed as part of Task A). Same precedent as `game/door.lua`'s deletion (see `docs/archive/design/puzzle-pile.md`).

- [x] Task C — `tests/test_jigsaw.lua` — Remove SpawnButton-specific test coverage now that `game/spawn_button.lua` no longer exists:
  - Delete the `-- SpawnButton --...` section and its `local SpawnButton = require("game/spawn_button")` line plus the two `do ... end` blocks that follow it: the `interact() invokes on_press exactly once per call` test and the `centre() returns sprite center` test (currently lines 1866–1891).
  - In the `GameScene:_spawn_box() places boxes on grid-aligned, in-bounds, non-colliding cells` test, remove the assertion `assert(not (bx == gs.spawn_button.sprite.x and by == gs.spawn_button.sprite.y), "box position should not collide with the spawn button's position")` (currently lines 1933–1934). Do not replace it with a `gs.pile` equivalent — the pile's non-collision is already covered elsewhere via the `self.pile.sprite.x == cx and self.pile.sprite.y == cy` occupancy clause in `_spawn_box()`.
  - Delete the two `Player:update` tests that exercise `button:interact()` via `SpawnButtonMod`: `Player:update calls button:interact() when no piece/box is in range but the button is` and the priority test `...and does NOT call button:interact() when a box interaction already happened on the same press` (currently lines 2103–2152, including their `local SpawnButtonMod = require("game/spawn_button")` lines). This priority coverage (pile loses to a nearby waiting box) is being re-added against `PuzzlePile` in Task D — do not leave a gap, just don't duplicate it here.
  - Leave every other test in the file untouched.

- [x] Task D — `tests/test_puzzle_pile.lua` — Add interact()/centre() coverage for `PuzzlePile`, mirroring the `SpawnButton` tests being removed from `tests/test_jigsaw.lua` in Task C:
  - An `interact()` test: construct `PuzzlePile.new(0, 0, function() calls = calls + 1 end)`, call `:interact()` twice, assert the callback fired once per call (mirror the old `spawn_button: interact() invokes on_press exactly once per call` test, using `PuzzlePile.new(x, y, on_press)`'s new third arg — note existing tests in this file call `PuzzlePile.new(x, y)` with only two args, which must still work since `on_press` is optional/nil-safe).
  - A `centre()` test: construct `PuzzlePile.new(320, 640, function() end)`, call `:centre()`, assert `c.x == 320 + C.U` and `c.y == 640 + C.U` (mirror the old `spawn_button: centre() returns sprite center` test).
  - A player-priority test confirming a nearby waiting box still wins over the pile when both are in interact range on the same press — mirror the box-vs-button priority test being removed from `tests/test_jigsaw.lua` in Task C (`require("game/player")`, `require("lua/headless/input")`, `require("game/jigsaw_box")`, `require("game/game_state")`; construct a `PuzzlePile` at the player's centre with an `on_press` callback that increments a counter, construct a `JigsawBox` at the same centre, press interact, assert the box's state becomes `"ejecting"` and the pile's callback fired 0 times). Also add the mirror of the "pile fires when nothing else is in range" case (construct only the pile in range, press interact, assert the callback fired once) — together these two replace both removed `Player:update` tests, now driven through `PuzzlePile` instead of `SpawnButton`.
  - This file's existing tests (remaining_puzzle_count, PuzzlePile:count(), PuzzlePile:top_position()) are untouched — just append the new tests.

- [x] Task E — `README.md` — Update documentation to reflect the merged pile/button:
  - In the world-overview paragraph (currently starts "A red **spawn button** sits at the top-left..."), remove the spawn-button sentence ("A red **spawn button** sits at the top-left of the (1280×640) world... Walk up to it and press **E** to spawn a brand-new gold box, letting you generate additional puzzles on demand.") and fold "walk up and press E to spawn a new box" into the puzzle pile's own description, which currently reads "A purely-visual **puzzle pile** sits at the top-centre of the world... the pile has no interaction of its own." — update that sentence so the pile is no longer described as purely visual / no-interaction, and instead describes walking up to it and pressing **E** to spawn a new gold box (same silent-no-op-at-cap and silent-no-op-when-exhausted behavior currently attributed to the spawn button should move to the pile's description too).
  - In the file-listing section, remove the `spawn_button.lua SpawnButton entity — ...` line entirely, and update the `puzzle_pile.lua Puzzle pile entity — visual-only, no interact()/state; ...` line to drop "visual-only, no interact()/state" and instead note it now owns the spawn interaction (`interact()`/`centre()`, fires an `on_press` callback to spawn a new `JigsawBox`), keeping the existing description of `count()`/`top_position()` and the stack-of-boxes rendering.
  - Do not change any other section of the README.
