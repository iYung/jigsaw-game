## Save / Load Checklist

- [x] Task A — `lua/core/save.lua` — **New file, no dependencies.** Create this file as a near-verbatim port of `/root/wip/lua/core/save.lua`, keeping only the generic parts this game needs (drop `Save.settings_exist()`/`Save.write_settings()`/`Save.read_settings()` entirely — per the design doc this game has no settings menu, just one `save.dat`). Port the local `serialize(val)` function unchanged (handles `nil`/`number`/`boolean`/`string` via `tostring`/`string.format("%q", ...)`, and `table` by walking `pairs(val)` and emitting `[k]=v` for integer keys, bare `k=v` for identifier-shaped string keys matching `^[%a_][%w_]*$`, and `["k"]=v` for other string keys — this covers every shape `GameState:to_save()`/`JigsawBox:to_save()`/`JigsawPiece:to_save()`/`GameScene:to_save()` will produce: nested tables, integer-indexed arrays like `pieces_to_spawn`/`pieces`/`boxes`/`completed_puzzles`, and string-keyed maps like `seen`/`solved_by_tier`). Expose exactly three functions: `Save.exists()` returning `love.filesystem.getInfo("save.dat") ~= nil`; `Save.write(data)` calling `love.filesystem.write("save.dat", "return " .. serialize(data))`; and `Save.read()` reading `love.filesystem.read("save.dat")`, returning `nil` immediately if the content is falsy, otherwise using `local loader = loadstring or load` and `pcall`-wrapping `loader(content)()`, returning `nil` on any `pcall` failure (corrupt/truncated save file) instead of erroring. Return the `Save` table at the end of the file.

- [x] Task B — `conf.lua` — **No dependencies.** Add one line inside the existing `function love.conf(t)` body (currently lines 9-27): `t.identity = "jigsaw_game"`. Place it near the top of the function, e.g. right after `t.window.title = ...` if this repo's `conf.lua` sets a title, or otherwise right after the `function love.conf(t)` line. This gives the game its own save directory under LÖVE's per-OS app-data folder so `love.filesystem.write("save.dat", ...)` (added in Task A/H) doesn't collide with any other unidentified LÖVE project on the same machine. Do not touch the existing `--headless` detection block or the `t.modules.*` toggles below it — leave those exactly as they are.

- [x] Task C — `game/game_state.lua` — **No dependencies.** Add two new methods to the `GameState` table, placed after `GameState:reset()` (currently lines 105-110) and before the final `return GameState.new()` (currently line 115). Add `function GameState:to_save() return { version = 1, seen = self.seen, solved_count = self.solved_count, active_count = self.active_count, solved_by_tier = self.solved_by_tier } end`. Add `function GameState:apply_save(data) if not data or data.version ~= 1 then self:reset() return end self.seen = data.seen self.solved_count = data.solved_count self.active_count = data.active_count self.solved_by_tier = data.solved_by_tier end` — note it mutates the singleton's fields in place (assigning into `self.seen` etc.) rather than replacing `self` itself, because every other module did `local GameState = require("game/game_state")` and holds a reference to this exact singleton table; replacing `self` would leave those references stale. Also update the file's header comment (currently lines 1-4, which says "Nothing here touches disk (no to_save/from_save, unlike ../wip's GameState -- persistence is explicitly out of scope)") since that statement is no longer true — reword it to note that `to_save()`/`apply_save()` now exist for the save/load feature, mirroring `../wip`'s pattern, while everything else about the module (session-lifetime singleton, no other persistence) stays the same.

- [x] Task D — `game/jigsaw_piece.lua` — **No dependencies** (safe to implement before or in parallel with Task E — see the shared-field note below). In `JigsawPiece.new(x, color, visual)` (currently lines 9-23), inside the `if visual then ... end` block (currently lines 14-19, which sets `self.sprite.image`, `self.sprite.quad`, `self.row`, `self.col`), add one more line: `self.path = visual.path`. This is forward-compatible whether or not the caller's `visual` table has a `path` key yet (Task E is the one that adds `path` to the table `game/jigsaw_box.lua` passes in as `visual`/`spec`; until Task E lands, `visual.path` is simply `nil` here, which is harmless). Then add two new functions after `JigsawPiece:draw_ghost` (currently ends at line 78) and before `return JigsawPiece`: (1) `function JigsawPiece:to_save() return { path = self.path, row = self.row, col = self.col, rotation_step = self.rotation_step, x = self.sprite.x, y = self.sprite.y } end` — only ever called by `game_scene.lua` for pieces whose `state == "grounded"` (mid-`"vanishing"`/`"held"` pieces are handled specially by the caller, per the design doc, not by this method). (2) `function JigsawPiece.from_save(data)` which rebuilds a piece exactly as it looked when saved: load `local image = love.graphics.newImage(data.path)`, get `local imgW, imgH = image:getDimensions()`, build `local quad = love.graphics.newQuad(data.col * C.SLOT, data.row * C.SLOT, C.SLOT, C.SLOT, imgW, imgH)` (same math `game/jigsaw_box.lua`'s `JigsawBox.new` already uses per-cell), then `local piece = JigsawPiece.new(data.x, {1, 1, 1, 1}, { image = image, quad = quad, row = data.row, col = data.col, path = data.path })` (the `x` passed to `.new` only seeds `GROUND_Y` positioning; immediately after construction, explicitly set `piece.sprite.x = data.x; piece.sprite.y = data.y` to restore the exact saved position, since a piece may not be resting at `GROUND_Y`). Restore rotation by calling `piece:rotate()` in a loop `for _ = 1, data.rotation_step do piece:rotate() end` (there is no direct rotation_step setter — `rotate()` is the only way `sprite.rotation` gets kept in sync with `rotation_step`). Return `piece`. Shared-field note: this task and Task E must agree that the box-supplied visual/spec table's puzzle-image path field is named exactly `path` — both task descriptions specify this same name, so no runtime coordination between the two tasks is required.

- [x] Task E — `game/jigsaw_box.lua` — **No dependencies** (safe to implement before or in parallel with Task D). Three changes: (1) In `JigsawBox.new` (currently lines 10-75), immediately after `local self = setmetatable({}, JigsawBox)` (currently line 30), add `self.path = path` (the local `path` variable already exists at line 27 — `local path = chosen.path` — currently only used to load the image and mark it seen; this task additionally stores it on the instance). (2) In the same function's piece-slicing loop (currently lines 61-66), add `path = path` to the table literal at line 64 so it reads `self.pieces_to_spawn[#self.pieces_to_spawn + 1] = { image = puzzle_image, quad = quad, row = row, col = col, path = path }` — this is the field `game/jigsaw_piece.lua`'s `JigsawPiece.new` (Task D) reads as `visual.path` when `_eject_next` (line 154) constructs a piece from one of these spec tables; no other code touches this table shape. (3) Add two new methods after `JigsawBox:draw()` (currently lines 174-176) and before `return JigsawBox`: `function JigsawBox:to_save()` returning `{ path = self.path, tier = self.tier, state = (self.state == "flying" and "waiting" or self.state), target_x = self.target_x, target_y = self.target_y, pieces_to_spawn = (build a plain array of `{row = spec.row, col = spec.col}` by iterating `self.pieces_to_spawn` in order — do not serialize `spec.image`/`spec.quad`, they're not serializable and are rederived from `path` on load) }` — the `"flying" -> "waiting"` collapse means the box's in-flight hop position is never saved (purely cosmetic, per the design doc). `function JigsawBox.from_save(data, world_w, world_h)` reconstructs a box without touching `GameState` at all (the puzzle's path was already marked seen when the box was first created, and the queue is already shuffled, so `from_save` must not call `GameState:mark_seen` or re-shuffle): create `local self = setmetatable({}, JigsawBox)`, set `self.path = data.path`, `self.tier = data.tier`, `self.world_w = world_w`, `self.world_h = world_h`, `self.target_x, self.target_y = data.target_x, data.target_y`; build `self.sprite = Sprite.new(data.target_x, data.target_y, C.SLOT, C.SLOT)` and set `self.sprite.color = {1, 0.75, 0.2, 1}` (matching `.new`'s line 33) and `self.sprite.visible = true` (a restored box was never mid-eject-hidden unless `data.state == "ejecting"`, handled next); load `local puzzle_image = love.graphics.newImage(data.path)`, `self.image = puzzle_image`, compute `imgW, imgH = puzzle_image:getDimensions()`, and re-derive `self.cols`, `self.rows`, `self.piece_count` with the exact same math/asserts `.new` uses (currently lines 49-58); rebuild `self.pieces_to_spawn` by iterating `data.pieces_to_spawn` and for each `{row, col}` building `local quad = love.graphics.newQuad(col * C.SLOT, row * C.SLOT, C.SLOT, C.SLOT, imgW, imgH)` then appending `{ image = puzzle_image, quad = quad, row = row, col = col, path = data.path }` (same shape `.new` builds, in the same saved order — do not re-shuffle); set `self.spawned = {}` (left for the caller — `game/scenes/game_scene.lua`'s restore path in Task F — to populate with any already-grounded pieces belonging to this box, since a `JigsawPiece` doesn't carry a back-reference to its box); if `data.state == "ejecting"` then `self.state = "ejecting"`, `self.spawn_timer = 0.3`, `self.sprite.visible = false` (matching what live `:interact()` does), otherwise `self.state = "waiting"`, `self.spawn_timer = 0`. Return `self`.

- [x] Task F — `game/scenes/game_scene.lua` — **Sequential: depends on Task D and Task E** (calls `JigsawPiece:to_save()`/`JigsawPiece.from_save()` and `JigsawBox:to_save()`/`JigsawBox.from_save()`, and reads `piece.path`/`box.path`) and **Task C** (implicitly — the caller in Task G is expected to have already run `GameState:apply_save(data.game_state)` before constructing `GameScene.new(data.scene)`, so this task's restore logic assumes `GameState`'s counts are already correct and must not re-invoke `GameState:puzzle_started()`/`GameState:puzzle_solved()`/`GameState:mark_seen()` during restore — those are already baked into the restored `GameState`). Two parts: **(1) Accept optional saved data.** Change `GameScene.new()` (currently lines 14-18) to `GameScene.new(save_data)` and store it, e.g. `self._save_data = save_data`, before returning. In `GameScene:on_enter()` (currently lines 20-90), keep all of the existing fresh-game setup (player/background/floor/pieces tables/pile construction) exactly as-is for the `save_data == nil` case (this must remain byte-for-byte today's behavior so existing tests/callers that do `GameScene.new()` with no args are unaffected), but branch the box/piece/completed-puzzle/shelf-cursor setup (currently lines 60-90) on whether `self._save_data` is present. **When `self._save_data` is present**, instead of lines 68-89's fresh-box logic: (a) set `self.player.sprite.x, self.player.sprite.y = self._save_data.player.x, self._save_data.player.y`; (b) if `self._save_data.player.held_piece` is non-nil, `self.player.held_piece = JigsawPiece.from_save(self._save_data.player.held_piece)` then `self.player.held_piece:pick_up()` (sets `state = "held"`; do not add it to `self.pieces` or the drawer — held pieces are drawn directly by `Player:draw()`, never through the drawer, matching how a live pick-up removes a piece from both, per `game/player.lua`); (c) rebuild `self.pieces` by mapping `self._save_data.pieces` through `JigsawPiece.from_save` into a flat array — do NOT explicitly add these to `self.drawer` or `self.pieces_in_drawer`; leave both empty as `on_enter` already initializes them, because `GameScene:update(dt)`'s existing loop (currently lines 139-144: `for _, piece in ipairs(self.pieces) do if not self.pieces_in_drawer[piece] then self.drawer:add(...) ... end end`) already lazily adds any piece in `self.pieces` missing from `self.pieces_in_drawer` on the very first tick, so restored pieces get added automatically; (d) rebuild `self.boxes` by mapping `self._save_data.boxes` through `JigsawBox.from_save(box_data, self.world_w, self.world_h)`, and for each restored box explicitly `self.drawer:add(box, C.PRIORITY_PIECE)` (unlike pieces, boxes are NOT lazily added by `update()`, so this must happen here, matching what the fresh-game path does at lines 75/118); (e) for each restored box, populate `box.spawned` and a matching `self.active_puzzles` entry by filtering `self.pieces` (from step c) for `state == "grounded" and piece.path == box.path`, i.e. `local matched = {} for _, p in ipairs(self.pieces) do if p.path == box.path then matched[#matched+1] = p end end box.spawned = matched` then `self.active_puzzles[#self.active_puzzles+1] = { pieces = matched, piece_count = box.piece_count, solved = false, image = box.image, cols = box.cols, rows = box.rows, tier = box.tier }` (mirrors the shape built at lines 76-84/119-127; every saved box was mid-puzzle and unsolved by definition, since solved puzzles are shelved into `completed_puzzles` instead, so `solved = false` is always correct here); (f) rebuild `self.completed_puzzles` by mapping `self._save_data.completed_puzzles` (each `{path, x, y, cols, rows}`) into the same shelved-entry shape `GameScene:update` builds at lines 210-222 — load `local image = love.graphics.newImage(entry_data.path)`, create a fresh `Shader.load("assets/shaders/rounded_corners.frag")`, `:send("size", {entry_data.cols * C.SLOT, entry_data.rows * C.SLOT})`, `:send("uv_rect", {0,0,1,1})`, build the same `{ image, x, y, cols, rows, shader, draw = ... }` table, append to `self.completed_puzzles`, and `self.drawer:add(shelved, C.PRIORITY_PIECE)`; (g) restore the shelf cursor directly: `self.shelf_row_x, self.shelf_row_bottom, self.shelf_row_max_height = self._save_data.shelf_row_x, self._save_data.shelf_row_bottom, self._save_data.shelf_row_max_height` (instead of the fresh-game defaults `0, -C.SLOT, 0` at lines 64-66). Still construct `self.pile = PuzzlePile.new(...)` exactly as today in both branches (it has no serializable state of its own — `PuzzlePile:count()` is fully derived from `GameState` + `PuzzleCatalog` per its own doc comment, so it's automatically correct post-restore once `GameState` has been reapplied by the caller). **(2) Add `GameScene:to_save()`**, placed after `GameScene:draw()` (currently ends at line 248) and before `return GameScene`. First, add `path = box.path` to the two `active_puzzles` entry literals built at lines 76-84 and 119-127 (alongside `pieces`/`piece_count`/`solved`/`image`/`cols`/`rows`/`tier`), so each entry carries its source puzzle path through to shelving time. Second, factor the shelving logic already in `GameScene:update` (lines 189-228 — the `if entry.image and entry.cols and entry.rows then ... end` block that builds the `shelved` table, appends it to `self.completed_puzzles`, adds it to the drawer, and advances the shelf-row cursor) out into a new method `GameScene:_shelve(entry)` that takes an `active_puzzles` entry and does exactly what that block currently does, additionally copying `path = entry.path` onto the `shelved` table it builds; have `update()`'s loop call `self:_shelve(entry)` in place of the inlined block (pure refactor, same behavior). Third, in `to_save()` itself: per the design doc's confirmed "collapse to shelved" behavior, any `active_puzzles` entry with `entry.solved == true` at save time (i.e. mid-fade) must be shelved immediately rather than dropped — before building the returned table, loop over `self.active_puzzles` and call `self:_shelve(entry)` for every entry where `entry.solved == true`, exactly as if its fade had finished this instant (this mutates `self.completed_puzzles`/`self.shelf_row_x`/etc. in place, which is safe since `to_save()` is only ever called immediately before quitting). Now build the return value: `{ player = { x = self.player.sprite.x, y = self.player.sprite.y, held_piece = (self.player.held_piece and self.player.held_piece:to_save() or nil) }, pieces = (map `self.pieces` filtered to `state == "grounded"` through `:to_save()` — pieces that were `"vanishing"` belonged to entries just shelved above and are correctly omitted, since a shelved puzzle's individual pieces are never part of the saved `pieces` list, only its one `completed_puzzles` entry is), boxes = (map `self.boxes` through `:to_save()` — note any box whose puzzle was just shelved above must already have been removed from `self.boxes` by the time `to_save()` runs, same as it would be on the next real `update()` tick: after the shelving loop, also remove from `self.boxes` any box whose `path` matches a just-shelved entry's `path`, so a fully-solved box doesn't get saved as if it were still active), completed_puzzles = (map `self.completed_puzzles` through `{ path = shelved.path, x = shelved.x, y = shelved.y, cols = shelved.cols, rows = shelved.rows }`), shelf_row_x = self.shelf_row_x, shelf_row_bottom = self.shelf_row_bottom, shelf_row_max_height = self.shelf_row_max_height }`.

- [x] Task G — `game/scenes/start_scene.lua` — **Sequential: depends on Task A (`Save.exists()`/`Save.read()`), Task C (`GameState:apply_save`), and Task F (`GameScene.new(save_data)`).** Add `local Save = require("lua/core/save")` and `local GameState = require("game/game_state")` to the requires at the top (alongside the existing `Scene`/`Input`/`GameScene` requires, currently lines 1-3). Change `self.items = { "New Game", "Exit Game" }` (currently line 22) to `self.items = { "New Game", "Continue", "Exit Game" }`, making "Continue" the new item 2 and pushing "Exit Game" to item 3. Implement `StartScene:on_enter()` (currently a no-op at line 53): set `self._has_save = Save.exists()`. Update navigation so item 2 ("Continue") is skipped when `self._has_save` is false, following the same pattern as `/root/wip/lua/game/scenes/start_scene.lua`'s `_next_selectable(current, delta, has_save)` local helper (lines 62-70 there): add an equivalent local helper in this file and use it in place of the current plain modulo-wrap logic in `StartScene:update` (currently lines 68-73, `self.selected = (self.selected % #self.items) + 1` / `self.selected = ((self.selected - 2) % #self.items) + 1`), calling it as `self.selected = _next_selectable(self.selected, 1, self._has_save)` for down and `_next_selectable(self.selected, -1, self._has_save)` for up, where the helper skips index 2 (`s ~= 2 or has_save`) as it wraps. Update `StartScene:_confirm()` (currently lines 57-63, `if self.selected == 1 then ... elseif self.selected == 2 then love.event.quit() end`) to the new 3-item mapping: `selected == 1` ("New Game") stays `self.manager:switch(GameScene.new())` (no args — always today's fresh-game behavior, per the design doc's "What stays the same"; no `Save.exists()`-then-delete step is needed, since a save is only ever written at quit time and New Game overwrites it naturally at the next quit); `selected == 2` ("Continue") is a no-op if `not self._has_save`, otherwise `local data = Save.read()` (guard: if `data` is `nil`, e.g. read failed/corrupt, no-op instead of erroring), then `GameState:apply_save(data.game_state)` followed by `self.manager:switch(GameScene.new(data.scene))`; `selected == 3` ("Exit Game") becomes `love.event.quit()` (same call, just renumbered). Update `StartScene:draw()` (currently lines 79-97) so item 2 is drawn dimmed/disabled when `not self._has_save` — e.g. render its rect/label at a reduced alpha (similar spirit to `/root/wip`'s `0.4`-alpha dimming at lines 145-151 of its `start_scene.lua`, adapted to this file's flat-color `NORMAL_COLOR`/`SELECTED_COLOR` rectangles rather than button images) instead of the normal `NORMAL_COLOR`/`SELECTED_COLOR` branch. `_item_rect(i)`/`_point_in_rect`/`_to_logical`/`mousemoved`/`mousepressed` (currently lines 34-51, 99-121) need no changes — they already operate generically over `#self.items`, so a 3rd item is handled automatically by the existing loops.

- [x] Task H — `main.lua` — **Sequential: depends on Task A (`Save`), Task C (`GameState:to_save`), and Task F (`GameScene:to_save`).** Add `local Save = require("lua/core/save")` and `local GameState = require("game/game_state")` to the top-level requires (alongside the existing `SceneManager`/`GameScene`/`StartScene` requires, currently lines 25-27). Add a new `function love.quit()` (there is currently no `love.quit` in this file) after the existing `love.keypressed`/`love.mousemoved`/`love.mousepressed` handlers: `function love.quit() if manager.current and manager.current.to_save then Save.write({ game_state = GameState:to_save(), scene = manager.current:to_save() }) end end`. The `manager.current.to_save` duck-type check (rather than an explicit `is_a(GameScene)` check) means this only fires when the current scene actually has a `:to_save()` method — today that's only `GameScene` (Task F), never `StartScene` — so quitting from the start menu correctly does not write/overwrite a save. Do not add any settings-save call (mirroring `/root/wip/main.lua`'s `love.quit`, currently lines 255-263, minus its `Save.write_settings(ss:to_save())` line — this game has no settings state to persist).

- [x] Task I — `tests/test_save.lua` — **New file. Sequential: depends on Task A (`Save`) and Task C (`GameState:to_save`/`GameState:apply_save`).** Follow the exact structure and style of `/root/wip/tests/test_save.lua` (plain `assert`-based, `print("PASS: ...")` per case, no test framework, runs headless via `love --headless tests/test_save.lua` or via the auto-discovering `love --headless` with no file arg per `lua/headless/runner.lua`). At the top, `local Save = require("lua/core/save")` and `local GameState = require("game/game_state")`, then monkey-patch `love.filesystem` with an in-memory table exactly like the wip file's lines 4-10: `local _fs = {}`, `love.filesystem.write = function(path, content) _fs[path] = content end`, `love.filesystem.read = function(path) return _fs[path], _fs[path] and #_fs[path] or 0 end`, `love.filesystem.getInfo = function(path) return _fs[path] and { type = "file" } or nil end`, and `local function reset_fs() _fs = {} end` — call `reset_fs()` before each `do ... end` test block, never touching real disk. Cover, in order: (1) `Save.exists() == false` with no file; (2) `Save.exists() == true` after a `Save.write({x=1})`; (3) `Save.read() == nil` with no file; (4) `Save.read() == nil` after writing corrupt content directly via `love.filesystem.write("save.dat", "NOT VALID LUA }{{{")`; (5) a scalar round-trip test writing a hand-built table shaped like this game's actual save format — `{ game_state = { version = 1, seen = {easy={["assets/puzzles/easy/1.png"]=true}, med={}, hard={}}, solved_count = 2, active_count = 1, solved_by_tier = {easy=2, med=0, hard=0} }, scene = { player = {x=320, y=192, held_piece=nil}, pieces = {}, boxes = {}, completed_puzzles = {}, shelf_row_x=0, shelf_row_bottom=-64, shelf_row_max_height=0 } }` — through `Save.write`/`Save.read` and asserting every field round-trips (nested `seen.easy` string-keyed table included); (6) `GameState:to_save()`/`GameState:apply_save()` round-trip: `GameState:reset()`, drive it via its real public API (`GameState:mark_seen("easy", "path.png")`, `GameState:puzzle_started()`, `GameState:puzzle_solved("easy")`), call `local saved = GameState:to_save()`, `Save.write({game_state = saved})`, `local loaded = Save.read()`, `GameState:reset()` (to prove `apply_save` — not stale in-process state — is what restores it), `GameState:apply_save(loaded.game_state)`, then assert `GameState.solved_count`/`GameState.active_count`/`GameState.solved_by_tier.easy`/`GameState:is_seen("easy", "path.png")` all match what was set before saving; (7) `GameState:apply_save(nil)` (or a `version`-mismatched table) falls back to `GameState:reset()`-equivalent zeroed state, per Task C's fallback behavior. End with `print("ALL TESTS PASSED")` matching the wip file's convention (line 295 there). Call `GameState:reset()` at the very end of the file too, so this test doesn't leave the process-lifetime `GameState` singleton dirty for whichever test file the runner happens to execute next (the existing `tests/test_game_state.lua` and `tests/test_jigsaw.lua` both already call `GameState:reset()` at the start of nearly every `do` block for exactly this reason, so this is consistent with the existing convention rather than a new one).

- [x] Task J — `tests/test_game_state.lua` — **Sequential: depends on Task C.** Add new `do ... end` blocks (following this file's existing convention of one scenario per block, `GameState:reset()` at the top of each, `assert(...)` with a descriptive message ending in `, got " .. tostring(...)`, and a trailing `print("PASS: game_state: ...")`) after the existing blocks (file currently ends at line 200 with the `solved_by_tier` reset test). Cover: (1) `GameState:to_save()` returns a table with `version == 1` and `seen`/`solved_count`/`active_count`/`solved_by_tier` matching the live singleton's current values after driving it via `mark_seen`/`puzzle_started`/`puzzle_solved`; (2) `GameState:to_save()` returns the *same* table references for `seen`/`solved_by_tier` as the live singleton (i.e. `saved.seen == GameState.seen`), documenting that `to_save()` is a shallow snapshot, not a deep copy — this matters because `lua/core/save.lua`'s serializer (Task A) is what actually produces an independent, disk-safe copy, not `to_save()` itself; (3) `GameState:apply_save(data)` mutates the existing singleton table in place rather than replacing it — capture `local ref = GameState` before calling `apply_save`, then assert `GameState == ref` afterward (this is the same invariant `tests/test_game_state.lua`'s very first block, lines 5-11, already checks for `require()` identity — this test extends that guarantee to `apply_save`); (4) `GameState:apply_save(data)` correctly restores `solved_count`/`active_count`/`solved_by_tier`/`seen` (including that `GameState:is_seen(tier, path)` reflects a restored `seen` table); (5) `GameState:apply_save(nil)` and `GameState:apply_save({version = 2, ...})` (wrong/missing version) both fall back to a freshly-reset state (`solved_count == 0`, `active_count == 0`, `solved_by_tier` all zero, nothing marked seen) rather than erroring or partially applying fields.

- [x] Task K — `tests/test_jigsaw.lua` — **Sequential: depends on Task D and Task E.** Add new `do ... end` blocks after the existing ones (file currently ends around line 1177+ with `Drawer`/pick-up/draw tests — insert the new blocks near the existing `JigsawBox`/`JigsawPiece` sections rather than at the very end, to keep related coverage together), following this file's existing helpers (`new_easy_box`, `unlock_all_tiers`) and conventions (`GameState:reset()` at the top of blocks that construct a box, descriptive `assert` messages, trailing `print("PASS: ...")`). Cover: (1) `JigsawPiece.new(x, color, visual)` with a `visual` table that includes a `path` field sets `self.path` to that value (extends the existing `JigsawPiece.new` tests around line 26); (2) `JigsawPiece:to_save()` on a grounded piece returns `{path, row, col, rotation_step, x, y}` matching the piece's current fields exactly (construct with a `visual` including `path`/`row`/`col`, rotate it once or twice, then check `to_save()`'s output against `piece.path`/`piece.row`/`piece.col`/`piece.rotation_step`/`piece.sprite.x`/`piece.sprite.y`); (3) `JigsawPiece.from_save(data)` round-trip: build a `data` table by hand (`path` pointing at a real catalog asset such as `"assets/puzzles/easy/1.png"`, arbitrary `row`/`col`/`rotation_step`/`x`/`y`), call `JigsawPiece.from_save(data)`, and assert the returned piece's `sprite.x`/`sprite.y` match `data.x`/`data.y` exactly (not `GROUND_Y`), `rotation_step` matches `data.rotation_step`, `sprite.rotation` equals `data.rotation_step * (math.pi/2)`, and `sprite.image`/`sprite.quad` are both non-nil (mirroring the existing "ejected piece carries non-nil image + quad" test's assertion style, around line 703-715); (4) a full `to_save()` → `from_save()` round trip on a single piece: construct one via `JigsawPiece.new(...)` with a `path`, mutate its position/rotation, call `:to_save()`, feed the result straight into `JigsawPiece.from_save()`, and assert the reconstructed piece's `sprite.x`/`sprite.y`/`rotation_step` match the original; (5) `JigsawBox:to_save()` returns `path`/`tier`/`target_x`/`target_y` matching the box, `state == "waiting"` for a box that was never `interact()`-ed (using `new_easy_box`), and a `pieces_to_spawn` list of 9 `{row, col}` pairs covering the same 9 cells the existing "pieces_to_spawn covers all 9 grid cells exactly once" test already verifies (reuse that test's `cell_of`-spying technique, around lines 582-625, adapted to check `to_save()`'s output instead of `box.pieces_to_spawn` directly); (6) `JigsawBox:to_save()` on a box whose `state == "flying"` (built via `new_easy_box(x, y, w, h, spawn_from)`) reports `state == "waiting"` in the saved data (the flying-to-waiting collapse) rather than `"flying"`; (7) `JigsawBox:to_save()` on an `"ejecting"` box (after `:interact()`, before all 9 pieces are out) reports `state == "ejecting"` and a `pieces_to_spawn` list shorter than 9, matching however many remain; (8) `JigsawBox.from_save(data, world_w, world_h)` round trip: take a fresh `new_easy_box`'s `:to_save()` output, feed it to `JigsawBox.from_save`, and assert the reconstructed box has matching `path`/`tier`/`target_x`/`target_y`/`cols`/`rows`/`piece_count`/`state`, that `#reconstructed.pieces_to_spawn == 9`, and that each reconstructed `pieces_to_spawn` entry carries non-nil `image`/`quad` (i.e. the queue was correctly rebuilt from `path`, not merely copied); (9) `JigsawBox.from_save` on data saved from an `"ejecting"` box restores `state == "ejecting"` and `spawn_timer == 0.3`, and a subsequent `:update(0.3, {})` call successfully ejects the next queued piece exactly like a live box would (proving the rebuilt `pieces_to_spawn` entries are actually usable by `_eject_next`, not just structurally present); (10) `JigsawBox.from_save` does not call `GameState:mark_seen`/`GameState:puzzle_started` — assert `GameState.active_count`/`GameState:is_seen(tier, path)` are unchanged by the `from_save` call itself (only by whatever the test explicitly drove beforehand).

- [x] Task L — `tests/test_start_scene.lua` — **Sequential: depends on Task G.** This file's existing tests (currently 143 lines) hard-code the 2-item menu (`"New Game"`/`"Exit Game"` at indices 1/2) and must be updated for the new 3-item menu (`"New Game"`/`"Continue"`/`"Exit Game"` at indices 1/2/3), in addition to adding new Continue-specific coverage. At the top, add `local Save = require("lua/core/save")` and stub `love.filesystem` in-memory exactly as Task I's `tests/test_save.lua` does (`local _fs = {}`, `write`/`read`/`getInfo` monkey-patches, a `reset_fs()` helper) so `Save.exists()`/`Save.read()` never touch real disk in this file either — call `reset_fs()` at the start of every test that constructs a `StartScene` (including the pre-existing tests), and remember every `StartScene.new(manager)` call must be followed by `scene:on_enter()` before `scene:update(dt)`/`tap(...)` is used, since `_has_save` is now computed in `on_enter()` (Task G), not in `.new()` — the existing tests currently never call `on_enter()` and will need this added. Update **Test 2** ("pressing down wraps 1 -> 2 -> 1", lines 32-44) to account for the new middle item: with no save present (`reset_fs()`, so `_has_save == false`), pressing down from 1 should skip index 2 (Continue, disabled) and land on 3 (Exit Game), and pressing down again should wrap to 1 — update the assertions and the test's own description accordingly. Update **Test 3** ("pressing up from 1 wraps to 2", lines 46-55) similarly: with no save, pressing up from 1 should wrap to 3 (Exit Game), skipping Continue. Update **Test 5** ("confirming while selected == 2 calls love.event.quit", lines 77-102) to select index 3 instead of 2 (via two down-taps, or by directly navigating with the save-aware skip logic) before confirming Exit Game. Update **Test 6** (mousemoved hit-testing, lines 104-123) and **Test 7** (mousepressed, lines 125-140) if they reference item-2/item-3 rects by position — Test 6 currently hovers what was item 2's rect (now Continue's rect); either repoint it at item 3's rect (new Exit Game position, `y = 340 + 2*(60+20) + 30 = 530`) or add a new assertion alongside it confirming hovering item 2's rect selects Continue. Then add new blocks: (a) with `reset_fs()` (no save) and `on_enter()` called, `scene._has_save == false` and confirming while `selected == 2` (Continue) is a no-op — assert `manager.switch` was never called (use a `switched_with` capture like the existing tests); (b) with a save present — write a valid save-shaped table via `Save.write({...})` matching the `{game_state=..., scene=...}` shape from Task H, using real `GameState`/`GameScene` requires — after `reset_fs()`, `Save.write(...)`, construct a fresh `StartScene.new(manager)`, call `on_enter()`, assert `scene._has_save == true`; (c) continuing from (b), confirming with `selected == 2` calls `manager:switch` with a `GameScene`-shaped object (has `.camera`/`.drawer`, same style as the existing New-Game assertion at lines 70-72) and that `GameState`'s fields reflect the restored save afterward (e.g. `GameState.solved_count` matches what was written); (d) the down/up navigation skip-logic test itself: with a save present, pressing down from 1 lands on 2 (Continue no longer skipped); without a save, it skips straight to 3.
