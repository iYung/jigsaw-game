## Start Scene Background Checklist

> **Sequencing note:** Task B depends on Task A (the PNG file must exist at
> `assets/backgrounds/start_bg.png` before `start_scene.lua` loads it via
> `love.graphics.newImage`). Run Task A first and confirm the file exists,
> then run Task B. Do not parallelize these two.

- [x] Task A — `assets/backgrounds/start_bg.png` (new file; generated via a throwaway script, script itself not committed) — Generate a flat/simple placeholder PNG (solid color or subtle gradient, no pattern) sized exactly **1280x720** px, matching the flat-placeholder style already used for `assets/backgrounds/floor.png`, `assets/backgrounds/world_bg.png`, and `assets/ui/panel_normal.png`/`panel_selected.png`. Use a short Python + Pillow script (same tooling precedent as the panel PNGs — see `docs/archive/design/menu-ui-pngs.md` and `docs/archive/design/floor-png.md`), run it once to write the PNG to `assets/backgrounds/start_bg.png`, then discard the script (do not commit it). Verify the output is exactly 1280x720 (e.g. `python3 -c "from PIL import Image; print(Image.open('assets/backgrounds/start_bg.png').size)"` should print `(1280, 720)`).

- [x] Task B — `game/scenes/start_scene.lua` — **(sequential; requires Task A's PNG to exist first)** Add a module-level image load next to the existing `PANEL_NORMAL`/`PANEL_SELECTED` loads (currently at `:19-20`): `local BACKGROUND = love.graphics.newImage("assets/backgrounds/start_bg.png")`. Then in `:draw()` (currently `:182-211`), add `love.graphics.draw(BACKGROUND, 0, 0)` (preceded by `love.graphics.setColor(1, 1, 1, 1)`) as the very first thing drawn — before the `"Jigsaw"` title `printf` and before the menu item loop — so the background renders underneath everything else. Draw at `(0, 0)` with no scale arguments (the image is already 1280x720, matching `LOGICAL_W x LOGICAL_H` exactly) — mirrors `floor.png`'s `love.graphics.draw(self.image, 0, 0)` call, not the panels' scaled draw calls. Make no other changes to the file — layout constants, input handling, item rects, and selection logic all stay untouched. No test changes are required: `lua/headless/stubs.lua` already stubs `love.graphics.newImage`/`love.graphics.draw` as no-ops, and `tests/test_start_scene.lua`'s existing `:draw()` smoke-call (around `:869-890`) will exercise the new draw call without modification.
