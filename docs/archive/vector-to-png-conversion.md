## Vector-to-PNG Conversion Checklist

- [x] Task A — `assets/ui/pile_box.png`, `assets/ui/wall_view_tile.png`, `assets/ui/solid.png` — Generate three new placeholder PNGs via a throwaway Python + PIL script (not committed, same precedent as `floor.png`/`panel_normal.png`): `pile_box.png` and `wall_view_tile.png` sized `C.PILE_BOX_SIZE` px square (currently `SLOT` = 64; check `game/constants.lua` to confirm), `pile_box.png` colored RGB 255,191,51 (matches today's `love.graphics.setColor(1, 0.75, 0.2, 1)`, fully opaque), `wall_view_tile.png` colored RGB 51,153,230 (matches `setColor(0.2, 0.6, 0.9, 1)`, fully opaque). `solid.png` is a small (e.g. 8x8) flat plain white square, fully opaque, no size dependency since it's always scaled to fit on draw. No border/gradient/rounded corners on any of the three — flat fill only. See `docs/design/vector-to-png-conversion.md` for full context.

- [x] Task B — `game/puzzle_pile.lua` — Add a module-level image load `local PILE_BOX = love.graphics.newImage("assets/ui/pile_box.png")` near the top of the file (alongside existing `require`s). In `:draw()` (currently `:58-69`), replace the `love.graphics.setColor(1, 0.75, 0.2, 1)` + `rectangle("fill", self.sprite.x + inset, by + inset, C.PILE_BOX_SIZE, C.PILE_BOX_SIZE)` pair inside the `for i = 1, n do` loop with `love.graphics.setColor(1, 1, 1, 1)` (no tint needed, color is baked into the PNG) then `love.graphics.draw(PILE_BOX, self.sprite.x + inset, by + inset, 0, C.PILE_BOX_SIZE / PILE_BOX:getWidth(), C.PILE_BOX_SIZE / PILE_BOX:getHeight())`. Keep the loop bounds, `inset`, and `by` math unchanged. Depends on Task A's asset existing at that path.

- [x] Task C — `game/wall_view_tile.lua` — Add a module-level image load `local TILE_IMAGE = love.graphics.newImage("assets/ui/wall_view_tile.png")`. In `:draw()` (currently `:33-40`), replace `love.graphics.setColor(0.2, 0.6, 0.9, 1)` + `rectangle("fill", self.sprite.x + inset, self.sprite.y + inset, C.PILE_BOX_SIZE, C.PILE_BOX_SIZE)` with `love.graphics.setColor(1, 1, 1, 1)` then `love.graphics.draw(TILE_IMAGE, self.sprite.x + inset, self.sprite.y + inset, 0, C.PILE_BOX_SIZE / TILE_IMAGE:getWidth(), C.PILE_BOX_SIZE / TILE_IMAGE:getHeight())`. Depends on Task A.

- [x] Task D — `game/scenes/settings_scene.lua` — Add a module-level image load next to the existing `PANEL_NORMAL`/`PANEL_SELECTED` loads (currently `:39-40`): `local SOLID = love.graphics.newImage("assets/ui/solid.png")`. In `:draw()` (currently `:290-304`): opaque-mode branch keeps `love.graphics.setColor(OPAQUE_BG_COLOR)` then replaces `rectangle("fill", 0, 0, LOGICAL_W, LOGICAL_H)` with `love.graphics.draw(SOLID, 0, 0, 0, LOGICAL_W / SOLID:getWidth(), LOGICAL_H / SOLID:getHeight())`; overlay-mode branch keeps `love.graphics.setColor(0, 0, 0, 0.55)` then the same `SOLID` draw call in place of its `rectangle` fill. Do not remove `OPAQUE_BG_COLOR` — it's still read by `setColor`. Depends on Task A.

- [x] Task E — `tests/test_puzzle_pile.lua`, `tests/test_wall_view_tile.lua` — Add one smoke test to each file asserting `pile:draw()` / `tile:draw()` (constructed the same way existing tests in that file already construct the object) doesn't error under the headless `love.graphics` stub, now that `draw()` calls `love.graphics.newImage`/`draw` instead of only `rectangle`. Match the style/precedent of `tests/test_settings_scene.lua`'s Test 25 (added for the menu-ui-pngs conversion — read it for the exact pattern: `pcall` wrapping the draw call, asserting `ok`). Depends on Tasks B and C landing first (the `draw()` code path must exist before testing it doesn't error, though the test itself would also pass against the old rectangle-based code — this ordering just keeps the checklist honest about intent). Also re-run `tests/test_settings_scene.lua`'s existing Test 25 to confirm it still passes once Task D lands — no new assertions needed there, per the design doc.
