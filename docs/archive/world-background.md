## World Background Checklist

- [x] Task A — `game/constants.lua` — Add named constants for the background's size and world-space offset instead of leaving them as magic numbers in `game_scene.lua`: `BG_W = 2496`, `BG_H = 1296`, `BG_OFFSET_X = -608`, `BG_OFFSET_Y = -328` (values per `docs/design/world-background.md`'s sizing derivation: `1280 + 2*608` x `640 + 2*328`, top-left at `(-608, -328)` so the floor rect sits centered inside it). Add them to the returned table alongside the existing `U`/`SLOT`/`PRIORITY_PIECE` entries.

- [x] Task B — `game/scenes/game_scene.lua` — (depends on Task A) In `on_enter` (~line 21-85), create `self.background` as a plain drawer entry loading `assets/backgrounds/world_bg.png` via `love.graphics.newImage`, with a `draw` function that sets color to white and draws the image at `(C.BG_OFFSET_X, C.BG_OFFSET_Y)`. Register it with `self.drawer:add(self.background, -1)` — priority `-1`, below the floor's `0` — and add this *before* `self.floor` is created/added (~line 33) so the background entity exists first in source order, matching the floor's existing pattern (`game_scene.lua:33-50`). No changes needed to `Scene:draw()` — it already wraps the whole drawer between `camera:attach()`/`detach()` (`lua/core/scene.lua:16-20`), so the background will pan with the world automatically.

- [x] Task C — `tests/test_jigsaw.lua` — (depends on Task B) Add a test block mirroring the existing "checkerboard floor" test (~line 2010-2041): call `GameScene.new()` / `gs:on_enter()`, then assert `type(gs.background) == "table"` and `type(gs.background.draw) == "function"`; search `gs.drawer.layers` for the entry whose `sprite == gs.background` and assert it exists with `priority == -1`; call `pcall(function() gs.background.draw() end)` and assert it doesn't error under the headless `love.graphics` stub (`lua/headless/stubs.lua`, which already path-aware-stubs `love.graphics.newImage`). Follow the file's existing style: a `do ... end` block with a short comment header referencing `docs/design/world-background.md` / `docs/checklists/world-background.md`, and a final `print("PASS: ...")` line.

- [x] Task D — `README.md` — (depends on Task B) Update the world description (around line 14, "A red **spawn button** sits at the top-left of the (1280×640) world...") to mention the new scrolling background drawn beneath the checkerboard floor, and update the assets section (around line 33, the `assets/` bullet listing `assets/shaders/`, `assets/puzzles/...`) to also list `assets/backgrounds/world_bg.png` and its purpose (placeholder background image, scrolls with the world, sized to cover the camera's worst-case overreach past the floor edges).
